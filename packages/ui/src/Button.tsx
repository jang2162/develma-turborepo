'use client';

import * as React from 'react';

export const Button = () => {
    return (
        <button className={'p-4 text-5xl  text-red-500 '} onClick={() => alert('boossp')}>
            Boop
        </button>
    );
};
